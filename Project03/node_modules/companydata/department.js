class department
{
constructor(dept_id, company, dept_name, dept_no, location){

    // Department Object
    var department = 
        {
            dept_id:this.dept_id,
            company:this.company,
            dept_name:this.dept_name,
            dept_no:this.dept_no,
            location:this.location  
        }  
    }

    // Getters
    getDeptID()
    {
        return this.dept_id;
    }

    getDeptCompany()
    {
        return this.dept_id;
    }

    getDeptName()
    {
        return this.dept_id;
    }

    getDeptNo()
    {
        return this.dept_id;
    }

    getDeptLocation()
    {
        return this.dept_id;
    }
}
